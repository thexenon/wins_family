header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All Scriptures
  .header__logo
    img(src='/img/favicons/logo.png' alt='Wins Family logo')
  nav.nav.nav--user
    a.nav__el.nav__el--cta(href='#') Stream
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el.nav__el--cta(href='/login') Log in